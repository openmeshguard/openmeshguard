Square icon-only button for toolbars and dense rows. Always give it a `title` for accessibility.

```jsx
<IconButton title="Filter" variant="secondary"><FilterIcon/></IconButton>
<IconButton title="More" variant="ghost"><MoreIcon/></IconButton>
```

Variants: `secondary`, `ghost`, `primary`. Sizes match Button (`sm` / `md` / `lg`).
