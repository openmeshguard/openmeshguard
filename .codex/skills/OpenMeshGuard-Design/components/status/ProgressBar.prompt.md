Coverage / posture bar with automatic threshold coloring (>=90 pass, >=60 warn, else fail).

```jsx
<ProgressBar value={82} />
<ProgressBar value={100} label="100%" />
<ProgressBar value={45} status="fail" height={6} />
```
