Toast notification with status accent and icon.

```jsx
<Toast status="pass" title="Scan complete" message="prod-eu-1 scanned. 3 new findings." onDismiss={close} />
<Toast status="fail" title="Export failed" message="Could not reach the cluster." />
```
