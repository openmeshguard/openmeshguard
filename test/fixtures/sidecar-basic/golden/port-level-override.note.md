# Golden note: port-level override

`port-level-override.json` must retain `mtls.effective: unknown` and the
workload-ports unavailable reason. `plan/M3-rule-engine.md` defers normalized
workload-port collection to a later milestone; M4 only proves that the live
fixture degrades honestly.
