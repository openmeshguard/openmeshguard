# Mixed mode

This namespace deliberately contains one ambient workload and one injected
sidecar workload. It proves sidecar precedence on the opted-in Pod template and
that inventory plus per-workload modes remain distinct in a single scan.
