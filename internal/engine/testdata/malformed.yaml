apiVersion: openmeshguard.io/v1alpha1
kind: ControlPack
metadata:
  name: deliberately-malformed
  version: 1.0.0
controls:
  - id: ACME-MTLS-001
    category: mtls
    severity: high
    evidenceType: runtime
    scope: workload
    requires: [mtls.effective]
    applicability: 'resource.kind == "Gateway"'
    expression: 'workload.mtls.effective =='
    message: 'Malformed control for validation diagnostics.'
    remediation:
      guidance: Fix the deliberately malformed test control.
    unexpectedField: true
