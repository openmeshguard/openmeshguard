Text input with label, hint, error, and optional leading icon.

```jsx
<Input label="Namespace" placeholder="e.g. payments" />
<Input label="Search" leadingIcon={<SearchIcon/>} size="sm" />
<Input label="Owner" error="Owner is required" />
```
