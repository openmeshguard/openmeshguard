Surface container with optional header (title, subtitle, actions).

```jsx
<Card title="mTLS coverage" subtitle="Across 6 production clusters" actions={<Button size="sm" variant="secondary">Export</Button>}>
  …body…
</Card>
<Card padded={false}><DataTable .../></Card>
```
